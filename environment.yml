name: sagemaker-local
channels:
  - defaults
dependencies:
  - python=3.10
  - scikit-learn
  - matplotlib
  - pandas
  - jupyterlab
  - pip
  - pip:
      - sagemaker
      - boto3
      - awscli
      - ipykernel
      - numpy

prefix: /opt/anaconda3/envs/sagemaker-local
